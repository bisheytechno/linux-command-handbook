# zcat Command

## What is zcat?

The `zcat` command displays the contents of compressed `.gz` files without extracting them.

## Syntax

```bash
zcat file.gz
```

## Example

```bash
zcat logs.gz
```

## Real World Example

```bash
zcat access.log.gz | less
```
