# zip Command

## What is zip?

The `zip` command compresses files and directories into a ZIP archive.

## Syntax

```bash
zip archive.zip file
```

## Example

```bash
zip project.zip report.txt
```

## Compress a Directory

```bash
zip -r project.zip myfolder
```

## Real World Example

```bash
zip -r backup.zip website/
```
