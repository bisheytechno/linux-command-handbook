# sudo Command

## What is sudo?

The `sudo` command executes commands with administrator privileges.

## Syntax

```bash
sudo command
```

## Example

```bash
sudo apt update
```

## Real World Example

```bash
sudo systemctl restart nginx
```
